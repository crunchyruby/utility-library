const uniqueArray = require('../array_utils/uniqueArray');

describe('uniqueArray', () => {
  test('should work correctly', () => {
    // Add your test cases here
    expect(uniqueArray('test')).toBeDefined();
  });
});