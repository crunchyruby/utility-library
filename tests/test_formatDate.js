const formatDate = require('../date_utils/formatDate');

describe('formatDate', () => {
  test('should work correctly', () => {
    // Add your test cases here
    expect(formatDate('test')).toBeDefined();
  });
});