const capitalizeFirstLetter = require('../string_utils/capitalizeFirstLetter');

describe('capitalizeFirstLetter', () => {
  test('should work correctly', () => {
    // Add your test cases here
    expect(capitalizeFirstLetter('test')).toBeDefined();
  });
});