const chunkArray = require('../array_utils/chunkArray');

describe('chunkArray', () => {
  test('should work correctly', () => {
    // Add your test cases here
    expect(chunkArray('test')).toBeDefined();
  });
});